# Persistent project context for Claude

Stop re-explaining your project every session. This guide shows a small, durable pattern: capture a project's essentials in one structured file, commit it to the repo, and pass it in the system prompt — so Claude is grounded from the first message.

## Contents

- `guide.ipynb`: Main tutorial notebook — a runnable before/after with the Anthropic SDK
